
<!DOCTYPE html> 
<html lang="en">
   <head>
      <title>Login</title>
   </head>
   <body style="padding-top:150px;">
      <div class="container">
         <center>
            <div class="container">
               <h2>BlogApp</h2>
               <div class="btn-group-vertical">
                  <%- if devise_mapping.omniauthable? %>
                  <%- resource_class.omniauth_providers.each do |provider| %>
                  <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider),:style=>"padding:5px 60px;",:class=>"btn btn-primary face" %><br />
                  <% end -%>
                  <% end -%>
                  
                 <!--  <button type="button" class="btn btn-danger goog">Google</button> -->
               </div>
               <h3>OR</h3>
              <%= form_for resource, as: resource_name, url: session_path(resource_name) do |f| %>
                  <div class="form-group">
                   <%= f.email_field :email, autofocus: true,:placeholder=>"Email",:id=>"email" %>
                   <br/><br/>
                   <%= f.password_field :password, autocomplete: "off",:placeholder=>"Password",:id=>"pass" %>
                   <br/>
                   <small>Need to BlogApp?<%= link_to "Sign up", new_registration_path(resource_name) %></small>
                 </div>
                 <%= f.submit ("Log in"),:style=>"padding:5px 70px;" %>
               <% end %>
            </div>
      </div>
      </center>
   </body>
</html>
<br/>
</div>
